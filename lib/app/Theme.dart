import 'package:flutter/material.dart';

class AppTheme{
  static final light = ThemeData(primaryColor: Colors.purple,
  brightness: Brightness.light);
  static final dark = ThemeData(
    primaryColor: Colors.purple,
    brightness: Brightness.dark
  );
}